#include <stdio.h>

/*
      Creator:    Aaro Salo
      Date:       28.11.2025
      Version:    0.001
      Other info: Basics to for loop
*/

int main(){
        int i; //Variables have to be declared
        int start = 0; //interger variable start value
        int stop = 10; //interger variable stop value

        //Loop is started from zero and it is stopped when it reacher more than nine
        for(i = start; i < stop;i++){
                printf("%d\n", i); //this prints value of i variable
